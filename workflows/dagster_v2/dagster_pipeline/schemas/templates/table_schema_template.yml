# Template for table schemas
# Copy this file to appropriate location and fill in details

metadata:
  version: 1.0.0
  created_by: "Your Name"
  description: "Brief description of the table"
  created_at: "YYYY-MM-DD"
  last_modified: "YYYY-MM-DD"
  # Optional: Add any additional metadata
  # owner: "team-name"

source:
  type: mysql  # or postgres, s3, api, clickhouse
  database: "database_name"
  table: "table_name"
  
  columns:
    # Required fields: name, type
    # Optional fields: nullable, primary_key, description, default, source_name
    
    - name: id
      type: bigint
      nullable: false
      primary_key: true
      description: "Primary key identifier"
      # source_name: "id"  # Use if source column name is different
    
    - name: created_at
      type: timestamp
      nullable: false
      default: CURRENT_TIMESTAMP
      description: "Record creation timestamp"
    
    - name: updated_at
      type: timestamp
      nullable: false
      default: CURRENT_TIMESTAMP
      on_update: CURRENT_TIMESTAMP  # MySQL specific
      description: "Record last update timestamp"
    
    # Example string column
    # - name: name
    #   type: varchar(255)
    #   nullable: false
    #   description: "User name"
    
    # Example decimal column
    # - name: amount
    #   type: decimal(18,2)
    #   nullable: false
    #   default: 0.00
    #   description: "Transaction amount"
    
    # Example enum column
    # - name: status
    #   type: enum('active','inactive','pending')
    #   nullable: false
    #   default: 'pending'
    #   description: "Record status"
    
    # Add more columns as needed...
