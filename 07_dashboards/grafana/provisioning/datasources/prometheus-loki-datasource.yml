apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:${PROMETHEUS_PORT}
    access: proxy
    isDefault: true
  - name: Loki
    type: loki
    url: http://loki:3100
    access: proxy