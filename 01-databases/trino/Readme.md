# Trino

- Trino is a distributed SQL query engine.

# Run Queries via Trino CLI (Inside Docker)

- Connect to the CLI:

  ```sh
    docker exec -it trino trino
  ```

- Test Connection to Catalogs
  ```sh
    SHOW CAATLOGS;
  ```
